body {
  margin: 0;
  padding: 0;
  background: #ccc;
}

section {
  margin-left: 20px;
}

h1 {
  font-size: 28px;
}

/* Styles the author text inside the header */
header > p {
  font-style: italic;
}

/* Styles the main blog content text */
article > p {
  font-size: 24px;
}
